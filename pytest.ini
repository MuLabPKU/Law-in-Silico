[pytest]
# Default pytest is the no-network release smoke surface.
# Legacy pollution unittest suites remain collectable under legal_simulation/testcases;
# run them explicitly while maintaining their older test harness assumptions.
testpaths = tests
